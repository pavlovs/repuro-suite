## Verdict
block — the migration strategy has data-integrity and rollout-order gaps that will misclassify legacy BA1-8 records and break BA9 ingestion paths.

## Critical issues
1. `ai/PLAN-M36.md:67` — Setting `prioritaet` default to `'Prio 1'` for all existing rows conflicts with `ai/PLAN-M36.md:28` (replace `klass IN ('A','B')` with `prioritaet='Prio 1'`): legacy `C/D/E/S` records would be treated as in-queue/enrich/export candidates. Suggested fix: add an explicit one-time backfill mapping (`A/B->Prio 1`, `S->Excluded`, non-target + `D/E` -> `Prio 2` or `Excluded`, `NULL->NULL`) and use temporary dual-read filters (`prioritaet='Prio 1' OR klass IN ('A','B')`) until backfill completion is verified.
2. `ai/PLAN-M36.md:89` — The ingest lines called out are wrong for BA9 category import; real Serienbriefe ingest still hard-whitelists old values at `src/pipeline/ingest.py:564-568`, so new codes from column F would be dropped to `NULL`. Suggested fix: update the Serienbriefe parser path itself with strict code mapping/validation and defined fallback for empty/unknown category values.
3. `ai/PLAN-M36.md:16` — New `prioritaet` column is specified without a coexistence/migration plan for existing `prio` usage already wired in API/UI/export (`src/pipeline/dashboard.py:86`, `src/pipeline/dashboard.py:749`, `src/pipeline/templates/dashboard_v2.html:2306`, `src/pipeline/templates/dashboard_v2.html:2782`, `src/pipeline/templates/dashboard_v2.html:3465`). Suggested fix: choose one canonical field (`prioritaet` or `prio`) and add an atomic schema + API + UI migration (including read/write compatibility window and data copy).
4. `ai/PLAN-M36.md:80` — SQL/JS coverage is not complete as written: many legacy filters/semantics remain outside the listed replacements (for example `src/pipeline/check_letter.py:221`, `src/pipeline/db.py:853`, `src/pipeline/db.py:858`, `src/pipeline/templates/dashboard_v2.html:1585`, and large legacy template surfaces in `src/pipeline/templates/dashboard.html` and `src/pipeline/templates/dashboard_v1.html`). Suggested fix: include an exhaustive file-by-file replacement checklist with a hard deprecation plan (remove `--v1`/legacy template serving if not migrated).
5. `ai/PLAN-M36.md:149` — Execution order is unsafe/inconsistent: T2 is scheduled before a defined data backfill for `prioritaet`, and phase text still references removed T7 at `ai/PLAN-M36.md:159`, creating breakable intermediate states. Suggested fix: use staged rollout gates: (a) schema + backfill + compatibility reads, (b) backend query switch, (c) UI switch, (d) compatibility cleanup after validation.
6. `ai/PLAN-M36.md:82` — Semantic mapping is underspecified for analytics/dropoff logic currently keyed to `D/E/S` vs `A/B/C` (`src/pipeline/dashboard.py:147-165`, `src/pipeline/templates/dashboard_v2.html:3445-3453`), so `prioritaet` does not fully replace overloaded meanings yet. Suggested fix: define a canonical semantic matrix (category, target-fit, exclusion reason, queue priority) and map each dashboard/export query to it explicitly.

## Secondary issues
1. `ai/PLAN-M36.md:72` — `N/A` is listed as valid category but omitted from planned UI options at `ai/PLAN-M36.md:122`. Suggested fix: either include `N/A` everywhere (UI/colors/validation) or remove it from allowed codes.
2. `ai/PLAN-M36.md:177` — Backwards-compat text says “until T7 migration runs” although T7 is removed at `ai/PLAN-M36.md:140-143`. Suggested fix: clean this contradiction and define end-of-compatibility conditions explicitly.
3. `ai/PLAN-M36.md:181` — Acceptance criteria are outcome-focused but lack concrete verification steps for regressions in `check_letter`, PDF/export paths, and JS behavior. Suggested fix: add a test matrix (SQL grep assertions, API integration tests, template smoke tests, BA1-8/BA9 coexistence fixtures).
4. `ai/PLAN-M36.md:16` — No value-level constraints are defined for the new text field(s), increasing drift risk from free-form UI edits. Suggested fix: enforce allowed values via DB `CHECK` constraints and server-side validation in PATCH handlers.
5. `ai/PLAN-M36.md:134` — BA9 introduction is not reflected in hardcoded batch ordering used by exports/UI (`src/pipeline/export_briefmarken.py:52`, `src/pipeline/dashboard.py:732-741`, `src/pipeline/templates/dashboard_v2.html:1339`). Suggested fix: include BA9+ ordering updates in scope or explicitly document why BA9 ordering is deferred.

## Checks passed
- `ai/PLAN-M36.md:15` correctly keeps the `klass` column name, which reduces blast radius for schema/API contracts.
- `ai/PLAN-M36.md:18` correctly preserves category while moving ownership exclusion intent away from category mutation.
- `ai/PLAN-M36.md:163-177` correctly prioritizes SQL/JS/template consistency and backward-compatibility as explicit review concerns.
