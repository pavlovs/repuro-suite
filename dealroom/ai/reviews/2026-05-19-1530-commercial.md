# Codex Review -- DR-GC Golden Spec Suite (Final Pass)
Date: 2026-05-19
Model: gpt-5.4 | Reasoning: high

## Verdict: REVISE

## Critical Issues (must fix before Roman reviews)
1. SPEC-MODEL still contradicts itself on the Bewertung write boundary. The sheet overview correctly says Bewertung is "MOSTLY FORMULA" and Roman sets manual valuation inputs (SPEC-MODEL.md:55), and the deep dive repeats that there are manual-input cells (SPEC-MODEL.md:110). But Hard Constraint 3 says Bewertung is "100% formula-driven" and only GuV-Konten is writable (SPEC-MODEL.md:547), and the confidence table says "Write confidence = NONE (formula-only)" (SPEC-MODEL.md:605). That reintroduces the exact boundary ambiguity the prior fix was meant to remove.
2. Offer-to-LOI pricing handoff is still not schema-consistent. SPEC-OFFER uses sofortzahlung_gross, earn_out_floor_ebit, earn_out_cap_ebit, and earn_out_type = "bracket_table" | "formula_cap" | "formula_floor_cap_minimum", while SPEC-LOI expects sofortzahlung, earn_out_floor, earn_out_cap, and earn_out_type = "bracket_table" | "formula_with_cap" | "formula_min_floor_cap" | "two_period". The DB dependency tables then drift further into generic names.
3. SPEC-NDA depends on NDA_Bilateral_Template.docx as the actual source template, but the same spec says that bilateral template is "NOT YET CREATED" and the manifest only registers nda/NDA_Template.docx plus signed deal files, not the cleaned bilateral template.

## Warnings (flag to Roman but not blocking)
1. Template path governance is still fragmented. RFI uses config/golden/rfi/, Offer uses dealroom/config/golden/offer/..., LOI/Model/Onepager use CLAUDE_REPURO/dealroom/config/golden/..., and NDA points to dealroom/templates/. The suite has not fully adopted the locked dealroom/config/golden/{type}/ convention.
2. Currency convention cleanup is incomplete. SPEC-MODEL still contains many EUR / K EUR unit references outside quoted workbook labels, and SPEC-RFI still uses K EUR in generated-question examples.
3. SPEC-ONEPAGER assumes python-pptx add_slide() can duplicate a complete source slide but does not define the actual full-slide clone mechanism.
4. Review gates are explicit in RFI, Offer, LOI, and NDA, but not yet as explicit in Onepager or Model. Onepager ends with "Report which elements need Roman's review" rather than a hard gate, and Model has Roman manual steps but no explicit final approval gate.

## Cross-spec Consistency
- Field naming: FAIL -- code_name is now consistent, but shared pricing fields are not. Offer and LOI still disagree on sofortzahlung vs sofortzahlung_gross, earn_out_floor vs earn_out_floor_ebit, earn_out_cap vs earn_out_cap_ebit, and the earn-out enum values.
- Currency formatting: FAIL -- legal-text convention is fixed in Offer/LOI, but technical prose still leaks EUR / K EUR in Model and RFI.
- Template paths: FAIL -- mixes config/golden/..., dealroom/config/golden/..., CLAUDE_REPURO/dealroom/config/golden/..., and dealroom/templates/.
- Terminology: FAIL -- mostly cleaned up but SPEC-RFI still uses non-identifier "generation" wording.
- Anti-patterns: FAIL -- Offer, LOI, Model, NDA, and Onepager have explicit anti-pattern coverage; RFI only links the anti-pattern doc in Cross-References.
- Review gates: FAIL -- explicit Roman gates exist in RFI, Offer, LOI, and NDA, but not as a hard final gate in Onepager or Model.

## Recommendation
Revise, then ship to Roman. Fix four things: unify Offer/LOI pricing schema, remove remaining Bewertung contradiction in Model, register bilateral NDA template under golden-corpus governance, and normalize path/currency/review-gate hygiene across RFI, Onepager, and Model.
